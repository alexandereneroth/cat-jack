'use strict';

// TODO: Is this really the best place for this function? 
// - i believe it's pretty decent atleast.
var cjTools = { //cj for CatJack

	isNumber: function (value) {
		return !isNaN(Number(value));
	}
};